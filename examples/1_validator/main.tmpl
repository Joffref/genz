// File generated by GenZ with template validator
package test

import (
  "fmt"
  "unicode"
)

func (v {{ .Type.Name }}) Validate() error {
{{ range .Attributes }} {{$attribute := .}}
  {{ if eq .Type.Name "string" }}
    {{ range .Comments }}
      {{ if eq . "+optional" }}
  if v.{{ $attribute.Name }} == "" {
    return fmt.Errorf("attribute '{{ $attribute.Name }}' must be set")
  }
      {{ end }}
      {{ if eq . "+startsWithCapital" }}
  if v.{{ $attribute.Name }} == "" || !unicode.IsUpper(rune(v.{{ $attribute.Name }}[0])) {
    return fmt.Errorf("attribute '{{ $attribute.Name }}' should start with a capital letter")
  }
      {{ end }}
    {{ end }}
  {{ end }}
  {{ if eq .Type.Name "int" "uint" "uint8" "uint32" "float32" "float64" }}
    // WORK IN PROGRESS
    {{ range .Comments }}
    {{ $comparators := split "," . }}
    // {{ $comparators }}
    {{ end }}
  {{ end }}
{{ end }}
  return nil
}
